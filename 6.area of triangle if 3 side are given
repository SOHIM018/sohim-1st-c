#include<stdio.h>
int main()
{
    float a, b, c, area, s;

    scanf("%f%f%f",&a, &b, &c);
    s=(a+b+c)/2;
    area=sqrt(s*(s-a)*(s-b)*(s-c));
    printf("area of the triangle=%f", area);

    return 0;
}
